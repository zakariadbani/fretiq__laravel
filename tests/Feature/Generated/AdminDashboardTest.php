<?php

// AUTO-GENERATED by /auto-improve-scaffold-tests (auth-aware) — per-route registration + gating smoke; real coverage lives in tests/Feature/Backend/.

namespace Tests\Feature\Generated;

use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

/**
 * Gating smoke: admin.dashboard — behind auth+verified+permission:backend.access.
 * Unauthenticated request must redirect (302) — never reach controller logic.
 */
class AdminDashboardTest extends TestCase
{
    use RefreshDatabase;

    public function test_unauthenticated_get_admin_dashboard_is_gated(): void
    {
        $response = $this->get('/admin/dashboard');
        $this->assertContains($response->status(), [302, 401, 403, 405]);
    }
}
