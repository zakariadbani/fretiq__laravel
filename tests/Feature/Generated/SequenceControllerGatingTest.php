<?php

// AUTO-GENERATED by /auto-improve-scaffold-tests (auth-aware) — per-route registration + gating smoke; real coverage lives in tests/Feature/Backend/.

namespace Tests\Feature\Generated;

use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

/**
 * Gating smoke: SequenceController routes — behind auth+verified+permission:backend.access.
 */
class SequenceControllerGatingTest extends TestCase
{
    use RefreshDatabase;

    public function test_unauthenticated_get_sequences_index(): void
    {
        $response = $this->get('/admin/sequences');
        $this->assertContains($response->status(), [302, 401, 403, 405]);
    }

    public function test_unauthenticated_get_sequences_create(): void
    {
        $response = $this->get('/admin/sequences/create');
        $this->assertContains($response->status(), [302, 401, 403, 405]);
    }

    public function test_unauthenticated_post_sequences_store(): void
    {
        $response = $this->post('/admin/sequences', []);
        $this->assertContains($response->status(), [302, 401, 403, 405, 419]);
    }

    public function test_unauthenticated_post_sequences_add_step(): void
    {
        $response = $this->post('/admin/sequences/1/steps', []);
        $this->assertContains($response->status(), [302, 401, 403, 405, 419]);
    }

    public function test_unauthenticated_delete_sequences_delete_step(): void
    {
        $response = $this->delete('/admin/sequences/1/steps/1');
        $this->assertContains($response->status(), [302, 401, 403, 405, 419]);
    }

    public function test_unauthenticated_post_sequences_pause_enrollment(): void
    {
        $response = $this->post('/admin/sequences/1/enrollments/1/pause', []);
        $this->assertContains($response->status(), [302, 401, 403, 405, 419]);
    }

    public function test_unauthenticated_post_sequences_resume_enrollment(): void
    {
        $response = $this->post('/admin/sequences/1/enrollments/1/resume', []);
        $this->assertContains($response->status(), [302, 401, 403, 405, 419]);
    }

    public function test_unauthenticated_post_sequences_stop_enrollment(): void
    {
        $response = $this->post('/admin/sequences/1/enrollments/1/stop', []);
        $this->assertContains($response->status(), [302, 401, 403, 405, 419]);
    }

    public function test_unauthenticated_put_sequences_execute_switch(): void
    {
        $response = $this->put('/admin/sequences/executeSwitch/1', []);
        $this->assertContains($response->status(), [302, 401, 403, 405, 419]);
    }

    public function test_unauthenticated_get_sequences_view(): void
    {
        $response = $this->get('/admin/sequences/1');
        $this->assertContains($response->status(), [302, 401, 403, 405]);
    }

    public function test_unauthenticated_get_sequences_edit(): void
    {
        $response = $this->get('/admin/sequences/1/edit');
        $this->assertContains($response->status(), [302, 401, 403, 405]);
    }

    public function test_unauthenticated_put_sequences_update(): void
    {
        $response = $this->put('/admin/sequences/1', []);
        $this->assertContains($response->status(), [302, 401, 403, 405, 419]);
    }

    public function test_unauthenticated_delete_sequences_delete(): void
    {
        $response = $this->delete('/admin/sequences/1');
        $this->assertContains($response->status(), [302, 401, 403, 405, 419]);
    }
}
