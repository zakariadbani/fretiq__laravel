<?php

// AUTO-GENERATED by /auto-improve-scaffold-tests (auth-aware) — per-route registration + gating smoke; real coverage lives in tests/Feature/Backend/.

namespace Tests\Feature\Generated;

use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

/**
 * Gating smoke: PlannerController routes — behind auth+verified+permission:backend.access.
 */
class PlannerControllerGatingTest extends TestCase
{
    use RefreshDatabase;

    public function test_unauthenticated_get_planner_index(): void
    {
        $response = $this->get('/admin/planner');
        $this->assertContains($response->status(), [302, 401, 403, 405]);
    }

    public function test_unauthenticated_get_planner_feed(): void
    {
        $response = $this->get('/admin/planner/feed');
        $this->assertContains($response->status(), [302, 401, 403, 405]);
    }
}
